name: Trigger Jenkins

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST -u ${{ secrets.JENKINS_USERNAME }}:${{ secrets.JENKINS_PASSWORD }} http://localhost:8080/job/myjob/build?token=${{ secrets.JENKINS_TOKEN }}
